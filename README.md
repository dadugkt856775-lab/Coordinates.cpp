# Coordinates.cpp
Given a matrix of size n × m and two coordinates (l1, r1) and (l2, r2), this program computes the sum of all elements inside the defined rectangle. It helps beginners understand 2D arrays, nested loops, coordinate-based traversal, and matrix manipulation.
